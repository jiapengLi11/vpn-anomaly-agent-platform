# Security and data policy

This repository contains only synthetic traffic metadata. It intentionally excludes packet captures,
trained weights, private knowledge documents, credentials, internal paths, and organization-specific code.

The demo classifier is not a security product. `CANDIDATE` means "investigate" and never means malicious.
The Agent fixes `securityVerdict` to `UNKNOWN` and does not perform automated blocking.
